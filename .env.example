# ── Milvus Vector Database ─────────────────────────────────
MILVUS_URI=http://localhost:19530
MILVUS_COLLECTION=k8s_failures

# ── Embeddings (bge-small-en-v1.5 — 384-dim, fast) ───────
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
EMBEDDING_DIMENSION=384
EMBEDDING_DEVICE=mps #Using MPS since apple silicon. If Nvidia, use CUDA. FOr cpu, use cpu

# ── Reranker (cross-encoder) ──────────────────────────────
RERANKER_MODEL=BAAI/bge-reranker-v2-m3 #Reranks our embeddings into top K relevant contexts
 
# ── LLMs via Ollama ───────────────────────────────────────
SIMPLE_MODEL=llama3.1:8b-instruct-q8_0 #Simple-> Small model
COMPLEX_MODEL=Qwen3-coder:30b #Complex -> Large model for complex queries
OLLAMA_BASE_URL=http://localhost:11434
QUERY_COMPLEXITY_THRESHOLD=0.5

# ── Retrieval ─────────────────────────────────────────────
CHUNK_SIZE=1000 
CHUNK_OVERLAP=200 
RETRIEVAL_TOP_K=4 

# ── Logging ───────────────────────────────────────────────
LOG_LEVEL=INFO
